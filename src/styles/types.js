// @flow

export type ColorProps = {
    primary? :boolean,
    accent? :boolean,
    secondary? :boolean,
    danger? :boolean,
    success? :boolean,
    color? :string,
}

export type ContainerProps = {
    color? :string,
    compact? :boolean,
    fluid? :boolean,
    className? :any,
}
