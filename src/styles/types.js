// @flow

export type ColorProps = {
    primary? :boolean,
    accent? :boolean,
    secondary? :boolean,
    danger? :boolean,
    success? :boolean,
    color? :string,
}

export type ContainerProps = {
    fluid? :boolean,
    compact? :boolean,
    className? :any,
}
