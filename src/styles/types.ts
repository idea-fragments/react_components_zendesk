export type ColorProps = {
    accent? :boolean,
    color? :string,
    danger? :boolean,
    primary? :boolean,
    secondary? :boolean,
    success? :boolean,
    warning? :boolean,
}

export type ContainerProps = {
    color? :string,
    compact? :boolean,
    fluid? :boolean,
    className? :any,
}
