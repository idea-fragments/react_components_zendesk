export type ColorProps = {
    accent? :boolean,
    color? :string,
    danger? :boolean,
    neutral? :boolean,
    primary? :boolean,
    // deprecated
    secondary? :boolean,
    success? :boolean,
    warning? :boolean,
}

export type ContainerProps = {
    color? :string,
    compact? :boolean,
    fluid? :boolean,
    className? :any,
}
